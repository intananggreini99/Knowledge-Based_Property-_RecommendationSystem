# Streamlit App

Run the Streamlit version from this folder:

```bash
pip install -r requirements.txt
streamlit run app.py
```

The original HTML/JS frontend is still preserved in:
`backend/app/static/` and `_original_frontend_backup/`.
