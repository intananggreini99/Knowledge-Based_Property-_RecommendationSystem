# URL of the FastAPI backend deployed on Railway.
# Leave EMPTY to run the site in offline "demo" mode (built-in sample data).
#
# Example (no trailing slash):
# NEXT_PUBLIC_API_URL=https://sistem-rekomendasi-properti-production.up.railway.app
NEXT_PUBLIC_API_URL=
